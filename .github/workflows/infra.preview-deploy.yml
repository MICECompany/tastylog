name: "infra preview deploy"

on:
  push:
    branches:
      - main
    paths:
      - 10_infra/*
      - .github/workflows/**
  workflow_dispatch:

permission:
  id-token: write
  contents: read

jobs:
  preview:
    name: "Preview"
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello World"

